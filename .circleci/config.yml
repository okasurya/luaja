version: 2

jobs:
  build:
    docker:
      - image: circleci/golang:1.12
    working_directory: /luaja
    
    steps:
      - checkout
      - run: GO111MODULE=on go mod download
      - run: go test ./... -cover


