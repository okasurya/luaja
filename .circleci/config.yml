version: 2

jobs:
  build:
    docker:
      - image: circleci/golang:1.12
    working_directory: /luaja
    
    steps:
      - checkout
      - run: go mod init
      - run: go mod tidy
      - run: go test ./...


